argon2-cffi==23.1.0
argon2-cffi-bindings==25.1.0
asgiref==3.12.1
cffi==2.1.1
Django==5.2.17
django-cors-headers==4.9.0
django-environ==0.11.2
django-unfold==0.104.1
djangorestframework==3.15.2
gunicorn==23.0.0
packaging==26.3
psycopg==3.3.4
psycopg-binary==3.3.4
pycparser==3.0
sqlparse==0.6.0
whitenoise==6.12.0

# --- Database drivers --------------------------------------------------------
# Only the one matching DATABASE_URL is actually loaded. psycopg for
# PostgreSQL; for MySQL, mysqlclient is faster but needs libmysqlclient at
# build time (`brew install mysql-client`, or default-libmysqlclient-dev on
# Debian). PyMySQL is pure Python and needs nothing, and settings.py falls back
# to it automatically when mysqlclient is not importable.
PyMySQL==1.2.0
# mysqlclient==2.2.7   # uncomment in an image that has the C client
